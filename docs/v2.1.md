# UNEEM v2.1

One mobile system across every route: restrained surfaces, clear type, shared spacing,
large primary actions, comfortable forms and immediate feedback. Keep the official
UNEEM identity, English/Arabic, account authority and booking rules intact.

## Scope and delivery

- [ ] Shared tokens, controls, sheets, dialogs, navigation, toast and loading states
- [ ] Login, registration, recovery, email confirmation, logout
- [ ] Home, facilities, availability, bookings, booking details, matches
- [ ] Profile, verification, pending access, help, reports and errors
- [ ] Every admin screen and its forms, filters, actions and empty states
- [ ] Notifications: badge, refresh, dismissal, failure and session isolation
- [ ] Asset quality, correctly sized icons, startup and navigation performance
- [ ] Responsive/RTL/theme/reduced-motion checks and keyboard layout validation
- [ ] Release checks, safe unused-code/branch cleanup, incremental commits and pushes
- [ ] Move the current DB connection into `.env.local`; keep secrets/local values out of Git

Branch: `codex/v2.1-unified-mobile`, based on `affcee4`.
Existing worktrees and branches with unique history must be preserved. Only remove
proven unused code or merged disposable branches; keep recovery refs for cleanup.

## Audit findings

- AuthShell overrides all theme colors and forces dark, unlike the rest of the app.
- TextField derives IDs from translated labels, uses 15px text and premature validation.
- Toast positions itself fixed inside a second fixed container, creating overlap;
  its close button does not notify the store, and two timers control its lifetime.
- Mobile spacing uses multiple gutter/padding owners and ignores visual viewport changes.
- Announcement state exists only inside the notification page, leaving badges stale elsewhere.
- First render waits on external Google font CSS; topbar downloads an 820KB torch for 28px.
- Existing shared controls still use different borders, heights, focus and pressed feedback.

## Verification

Use existing Svelte/Tailwind/Lucide/Playwright tooling. Browser skill is unavailable,
so rendered checks use Playwright. Test real route components with isolated API fixtures
where authenticated accounts are unavailable; fixture checks are not hosted E2E proof.
Save visual evidence and temporary scripts outside the repository. Physical iPhone keyboard,
installed PWA and hosted auth/notification delivery remain separate release gates.

References: [Visual viewport](https://developer.mozilla.org/en-US/docs/Web/API/VisualViewport),
[Apple buttons](https://developer.apple.com/design/human-interface-guidelines/buttons).

### Batch 1: shared UI and auth

Implemented shared button/link variants, 44px minimum targets, 56px primary form
actions, semantic field labels/IDs, calmer surfaces, self-hosted fonts, light/dark auth,
native dialogs, keyboard viewport handling, and stacked/dismissible top-layer toasts.
Login includes a full-width secondary Create account button. Registration headers
no longer overlap Back. Forms wait for hydration to prevent lost input/native submits.

The original brand vector now has an opaque background and a larger, mask-safe mark.
Regenerated 180/192/512px PNG exports replace the inconsistent installed app icons.
Topbar now uses the 3KB vector instead of the 820KB torch. Removed the blocking Google
stylesheet. Geist is served locally (29KB); Noto Sans Arabic loads for Arabic UI.
Mobile auth content starts below the header without vertical recentering. Forgot
password sits next to its field label; primary and secondary actions are grouped.

Local dependencies required a lockfile reinstall to restore missing Windows bindings.
Type checking reached zero errors/warnings. Production build passed. Visual checks:
390x844 English/Arabic, 320x420 short viewport, 1440x960 desktop; no overflow or page errors.
Tested password visibility, empty-form validation, independent toast dismissal/stacking,
and simulated visual-viewport keyboard state. This does not certify a physical iPhone.

Corrected two existing test-fixture mistakes: exact matching for the obsolete Ready
badge (instead of matching Already), and exposing the Auth API version header so the
Supabase SDK can read the mocked error code. No auth authority checks were relaxed.

Verification checkpoint: all 32 existing public smoke cases passed in a single run
after the font/position refinement. Targeted checks also cover the subsequent inert
shell during hydration, toast dismissal and keyboard simulation. Remaining routes,
notification behavior and production/physical-device gates are still open.

Safe local branch cleanup: `feature/new-frontend` and `wip-frontend` both pointed to
the fully merged `4bd1fd4`. Preserved them under
`refs/codex/recovery/v2.1-20260908/feature-new-frontend` and
`refs/codex/recovery/v2.1-20260908/wip-frontend` before deleting the branch names.
Kept main/dev, branches with unique history, and the existing worktrees.

### Batch 2: notifications

One session-scoped notification store now powers the app badge and notification
screen. Initial loading yields to the main screen; navigation reuses the same
result, and foreground/online refreshes recover stale data. Dismissal updates the
screen and count immediately, persists through the existing RLS-protected table,
and restores the item with a toast if the request fails. Sign-out aborts pending
loads and clears notification state. The screen uses the shared spacing/surfaces,
with a refresh action, readable errors, and a single empty state.

Added a real-screen Playwright scenario with intercepted API fixtures: login to
Home, badge before visiting Notifications, no duplicate load on navigation,
immediate dismissal before server response, failed-write rollback, refresh failure,
online recovery, and sign-out cleanup. Desktop and mobile passed; no hosted data
was written. Existing backend announcement/RLS contracts are unchanged.

### Batch 3: unified screens, Menu, invitations and identity approval

Reworked the Home/facility/match/booking cards, booking sheet, verification flow,
profile spacing, Help composer, report form and every admin route. Shared native
dialogs replace the remaining active custom modals; action errors retain entered
data. Ongoing bookings remain in upcoming sports, and history is no longer silently
cut to eight entries. Arabic, long names and narrow layouts use wrapping content.

The pitch page now uses compact cards with time, one status and a full-width
action. Capacity and opening hours appear once in the facility header. Menu
replaces Profile in both navigation layouts, grouping account, appearance,
language, support, installation and administration. The sidebar and account
dropdown are removed. Profile is reached from Menu.

Open booking details offer Invite friends: native sharing, clipboard confirmation,
or a selectable-link sheet when neither API is available. Shared booking links
return to the requested booking after sign-in; destinations are restricted to
internal booking/match UUID paths. No invitation sends automatically.

All students can browse after email confirmation; ID approval is required before
booking or participation, including USMBA accounts. Student IDs accept S/s plus
digits. A pending correction supersedes the old attempt, preserves its card and
history, and cannot race past an approval to replace a verified identity. A shared
sticky reminder remains present while verification is incomplete.

Hosted layer 060 was applied to `hudjpcrjoryyhpphonsp` as
`20260909163702_universal_student_verification_v21`. Its full universal verification
contract passed in the same transaction. Test fixtures were rolled back before
schema commit; post-checks confirmed no fixture users, facilities or storage rows.
The separate multi-session race gate has not been rerun. Advisor results contain
intentional RPC/RLS visibility notices, disabled leaked-password protection and
two existing reservation foreign-key index suggestions; this is not a security
sign-off. [Advisor reference](https://supabase.com/docs/guides/database/database-linter).

Both local env files now point to the current project. The retired project caused
the reported login DNS failure. Runtime inspection confirms the old URL is absent;
the local login page and current Auth settings endpoint return HTTP 200. Original
local values are preserved outside the repository under
`%LOCALAPPDATA%/Codex/recovery/uneem-v2.1-20260909`. No secrets are committed.

Removed four unreferenced brand exports (3.1 MB) and obsolete sheet/badge/skeleton
components after repository-wide reference checks. The language picker uses system
type so English auth does not need the full Arabic font merely for its picker.
First PWA installation no longer reloads an active form; update prompts can be
deferred and stay out of keyboard/dialog/toast interactions.

Verification checkpoint: production build and release hygiene pass; all 42 public,
notification, identity and unified-UI Playwright cases pass on desktop/mobile.
Coverage includes populated slots and matches, failed booking recovery, Menu
theme/language, invite return/fallback, admin routes and short-screen dialogs.
Visual review prompted a further intrinsic grid-width correction in the facility
form, to be checked with the next profile increment. Browser fixtures are distinct
from real mail delivery, physical keyboards, installed PWA sharing and release
deployment. Profile editing expansion and measured startup remain in progress.

### Continuation: profile editing and email changes (2026-09-10)

Resumed `codex/v2.1-unified-mobile` at `ecaed40`, preserving the unfinished profile
increment and all sibling worktrees. Profile now edits name/username through the
existing narrow RPC, retains input on save failure, and starts email/password
changes through Supabase Auth. Student ID correction leads to Verification;
approved IDs remain read-only. Disposed/session-switched profiles cannot publish
late errors. The admin facility grid fix is retained and whitespace corrected.

The real email confirmation route now accepts `email_change` tokens with the
correct OTP type, rejects unrelated flow types before calling Auth, and shows a
quiet pending state after the first secure-email confirmation. Callback tokens
are removed from the URL after acceptance. Profile and callback browser tests
cover success, failure, pending identity and wrong-flow tokens.

Live inspection: canonical Vercel project `uneem` (`prj_qn34V8McQxZNWmbdbAx8W1e9LWZr`)
has a READY `ecaed40` preview; production remains the promoted `8dc4a3d` deployment.
Both `https://uneem.site/login` and `https://www.uneem.site/login` returned HTTP 200.
The 24-hour runtime-error query returned only 404 probe/static-file groups, not an
application-route crash. The `booking` project slug returned 404 in the canonical
team; no stale integration configuration was modified.

Supabase public settings show email and Facebook enabled, Google disabled.
The current profile trigger requires a client-supplied username, so fresh OAuth
profiles need a compatible server-generated username before social entry is exposed.
The product owner's SMTP/template update is recorded in `v2/auth-runtime.md`;
custom hosted templates must not be replaced with older local email templates.

Required continuation remains one V2.1 release: provider sign-in/link/removal;
duplicate-ID recovery with server-side ownership validation; audited lost-access
review/relink preserving the original identity/history; measured startup; complete
responsive/RTL/PWA/asset review; and actual mailbox/provider/device release evidence.
No identity transfer or production promotion was performed in this checkpoint.

Checkpoint validation: production build and release hygiene passed; Svelte reported
zero errors/warnings; all 50 Playwright cases passed against the production build
using installed Chrome (desktop and Pixel 5 emulation, including English/Arabic).
The pinned Playwright binary was absent. Running dev and build together caused
local generated-file churn, so browser validation used `127.0.0.1:4173` preview.
Hosted profile fixtures were confirmed absent after the transactional contract.
Screenshots were inspected and kept outside Git in the task visualization folder.
This is local UI plus hosted SQL proof, not real mail/provider/physical-iPhone proof.

### Continuation: OAuth profile compatibility and sign-in

Login now groups Google/Facebook controls with the existing primary email sign-in.
One shared, non-blocking public settings read enables only configured providers;
email remains usable when settings fail. Reserved control space prevents layout
shift. Supabase owns the PKCE exchange and uses the existing `/verify-email`
callback; no custom credential or token service is introduced.

Layer 062 uses Supabase-owned provider metadata to assign a public username and
fallback name when social providers omit registration fields. Social metadata
cannot claim Student ID, admin role, approval or sports rights. Existing email
signup validation and all layer-060 invariants are preserved.

Validation: production build/type check/hygiene passed; 56 browser cases passed
on desktop and Pixel 5 using installed Chrome. Both provider authorization URLs,
PKCE challenges, unavailable-provider fallback and stable form position were
checked without provider credentials. OAuth and universal verification SQL
contracts passed with the proposed trigger in rollback transactions. Layer 062
was then applied as `20260910031617_oauth_profile_bootstrap_v21`; the OAuth
contract passed again and zero fixture users remained.

Security advisors introduced no new categories/counts. Existing notices remain:
RLS without policies on RPC-only tables; authenticated GraphQL schema visibility;
public/authorized SECURITY DEFINER API functions; and disabled leaked-password
protection. These are not a full security sign-off. Reference:
[database linter](https://supabase.com/docs/guides/database/database-linter) and
[password protection](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection).

The owner requires developer-controlled live accounts only, no invented
credentials/passwords in config, and only the human OAuth step to remain pending.
All independent work continues. Google is currently disabled in hosted settings;
Facebook/email are enabled. Provider consent, linking/removal and mail delivery
are still separate live gates. No production frontend promotion occurred.
